# Project-level R profile for CQC Statistical Guidance
#
# Python configuration:
#   The .Renviron file sets RETICULATE_PYTHON to the local Python path.
#   .Renviron is gitignored (machine-specific). To set up on a new machine:
#   1. Create .Renviron in the project root
#   2. Add: RETICULATE_PYTHON=/path/to/python
#   The Python environment needs: pandas, numpy, scipy, statsmodels
